functions-framework==3.*
flask>=3.0
torch>=2.4.0
torchvision>=0.19.0
opencv-python-headless>=4.9.0
numpy>=1.24
Pillow>=10.0
hydra-core>=1.3
omegaconf>=2.3
iopath>=0.1.10
einops>=0.7.0
tqdm>=4.65
imageio[ffmpeg]>=2.34.0
# SAM 2 from official Meta repo (no reliable PyPI wheel as of 2024)
# Note: we DO NOT use decord (no wheel for Cloud Run Linux). Instead we pre-decode
# the MP4 to a directory of JPEG frames with cv2, then pass the dir to init_state.
git+https://github.com/facebookresearch/sam2.git@main
